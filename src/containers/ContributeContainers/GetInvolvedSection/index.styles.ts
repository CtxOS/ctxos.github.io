import { CardActionArea, styled } from '@mui/material'

export const StyledGetInvlovedCardActionArea = styled(CardActionArea)(({ theme }) => ({
  borderRadius: theme.spacing(3),
  height: theme.spacing(35)
}))
